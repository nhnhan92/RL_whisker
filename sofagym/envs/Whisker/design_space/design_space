import gym

no_chamber = 3
body_length = [100, 90, 80]

def design_space():
    parameter_space = gym.spaces.MultiDiscrete(nvec=no_chamber*[len(body_length) + 1])
    print(parameter_space)

design_space